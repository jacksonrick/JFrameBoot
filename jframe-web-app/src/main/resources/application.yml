server:
  port: 8082
spring:
  profiles:
    active: dev
  application:
    name: JFRAME-WEB-APP
info:
  app:
    name: ${spring.application.name}
    version: "1.0"
    description: "app"
app:
  host: 192.168.1.199
  appkey: 00056cb1d74435076a4c15a490798df4
  static_path: /data/wwwroot/app/static/
  static_host: http://${app.host}:${server.port}/
  cache:
    enabled: true